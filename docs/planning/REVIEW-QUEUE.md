# Review Queue

Cross-week state for the recurring **Weekly Reviews** batch (see WEEKLY.md → Recurring Weekly Reviews / Group WR). Each week, per category, use **hybrid sourcing**: fresh-check the live landscape (a `WebSearch` for the current best, excluding the Reviewed log) **and** consider the parked **Next-up** item, then review whichever is strongest — do not rote-pick the parked item. Use **lightweight inline research** (a few `WebSearch` + 2–3 `WebFetch`; never the deep-research harness for a routine review). Append a verdict row (`adopt | pass | defer`); park notable runners-up under **Next-up**. On an `adopt`, also file a 🟤 Auto-Generated entry in BACKLOG.md **and name its trial vehicle** — see [§ 5. Adopt trials](#5-adopt-trials) for the policy and the outcome log.

**Sections 1–3 are inbound** (external candidates reviewed for use here). **Section 4 is outbound/inbound propagation** and uses a different verdict vocab (`propagate | pass | defer`) with a different routing sink (`TODO.md` § Spawned Tasks) — its conventions are stated in that section.

**Created**: 2026-06-19 (first run scheduled June 22–26, 2026 week — empty Reviewed logs to start).
**Methodology**: see the **Methodology (canonical — current practice)** section of [`docs/superpowers/specs/2026-06-26-weekly-reviews-first-run-design.md`](../superpowers/specs/2026-06-26-weekly-reviews-first-run-design.md) — the reusable "how we run Weekly Reviews" reference (hybrid sourcing, lightweight inline research, docs-only-PR handling, run-card path, verdict rubric).

---

## 1. Plugins

Review **two independent tops** each week: the best not-yet-reviewed plugin from the **official Claude plugin store**, and, separately, the best from the **wider internet**. Log each with its `source:`.

### Reviewed log

| Date       | Item                                                              | Source                                                                                                                       | Verdict   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-26 | pr-review-toolkit (official)                                      | store: [docs](https://code.claude.com/docs/en/discover-plugins) · `pr-review-toolkit@claude-plugins-official`                | **adopt** | Official Anthropic plugin with specialized PR-review agents (tests, silent-failure-hunter, type-design, code-quality, simplification); not yet used here and complements the existing `/code-review`. Verdict rests on official primary sources — harness adversarial-verification was rate/session-limited.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-06-26 | test-writer-fixer (community, via awesome-claude-plugins)         | wider: [repo](https://github.com/ComposioHQ/awesome-claude-plugins)                                                          | defer     | Vitest support fits this repo's 381-test unit suite, but it's unvetted third-party, and the community Playwright skills surveyed have an Electron-support gap (this repo's E2E target is Electron). Eval before adopt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-05 | typescript-lsp (official)                                         | store: [docs](https://code.claude.com/docs/en/discover-plugins#code-intelligence) · `typescript-lsp@claude-plugins-official` | **adopt** | Official code-intelligence LSP (TS **and** JS via `typescript-language-server`): automatic post-edit diagnostics (type/syntax/missing-import, same turn) + precise symbol navigation (go-to-def, find-refs, call hierarchy) on the ~8400-line renderer — hits the CLAUDE.md "renderer file is large" pain and the no-CI "errors surface late" gap. Untyped-JS reduces type-checking value (navigation + basic JS diagnostics still apply) and large-project LSP memory use are the trial eval points, not blockers → 🟤 filed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-05 | Electron Developer Agent (community, awesome-claude-code-toolkit) | wider: [repo](https://github.com/rohitg00/awesome-claude-code-toolkit) · `agents/core-development/electron-developer.md`     | pass      | A single generic senior-Electron **persona file** (not a plugin); its mandated defaults (`contextIsolation:true` + `sandbox:true` on every window, cross-platform macOS/Win/Linux testing) conflict with this project's intentional sandbox-disabled, Windows-only design, and its guidance is already exceeded by the repo's CLAUDE.md Electron gotchas. Not better than current practice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-27 | security-guidance (official)                                      | store: [docs](https://code.claude.com/docs/en/security-guidance) · `security-guidance@claude-plugins-official`               | **adopt** | **Reverses its own 2026-07-05 "low fit" parking** — the parked note reasoned "no CI + a solo dev already gated by ESLint + the pre-commit secret guard", but the primary doc's own defense-in-depth table shows the plugin occupies the **in-session** stage that neither ESLint nor the secret guard covers, and _no CI_ is an argument **for** it (there is no later net) rather than against. Three layers: a free deterministic per-edit pattern match (no model call), a background end-of-turn diff review, and an agentic review on each commit/push Claude makes. Three concrete hooks into this repo's actual surface: built-in DOM-injection patterns (`.innerHTML =`, `document.write`) land on the ~8400-line renderer; `child_process.exec` lands on the ffprobe/ffmpeg main-process calls; and `.claude/security-patterns.yaml` `paths` globs can finally make CLAUDE.md's advisory **"changes to preload.js require security review"** deterministic, with `.claude/claude-security-guidance.md` carrying the Electron threat model (`sandbox:false`, contextIsolation, the IPC bridge, file move/delete ops). Trial-eval points, not blockers: needs **Python 3.10+ on `PATH`** plus a first-run venv at `~/.claude/security/` (pip + network) — unverified on this Windows/Node box; model-backed layers cost ~1 review per file-changing turn + 1 per commit on Opus 4.7 by default (`SECURITY_REVIEW_MODEL`/`SG_AGENTIC_MODEL` to downgrade); and the commit layer sees only commits **Claude** makes via Bash, not the user's own shell commits. → 🟤 filed. |
| 2026-08-27 | dead-rules-audit (community, `karanb192/claude-code-hooks`)       | wider: [repo](https://github.com/karanb192/claude-code-hooks) — **arrived via the inbound propagation channel** (see §4)     | **adopt** | A CLAUDE.md **compliance scorecard**: parses rules at `SessionStart`, scores every `Edit`/`Write` against them in `PostToolUse`, and at `SessionEnd` renders a worst-first scorecard flagging chronically-ignored rules `⚠ promote→hook` (also on demand via `/dead-rules-audit:scorecard`). MIT, **488★**, **Node ≥18 with no npm dependencies**, platform-agnostic — no win32 blocker. Adopted because the fit was **measured, not assumed**: the sibling repo ran the plugin's own parser against this repo and got **36 rules / 10 judgeable** from `CLAUDE.md`, parsing whole atomic rules (`"Prettier: tabWidth=4, useTabs=false, singleQuote…"`), where its own hard-wrapped prose yielded 5 rules / 2 mid-sentence fragments — a structural mismatch there and a clean fit here. It targets this repo's best-documented chronic failure: advisory CLAUDE.md rules that keep being ignored (the doc-drift class recurring across PR #51→#52→#63 and still open as G4's 🟤; the "grep the repo for the symbol" rule whose breach caused the PR #56 follow-ups). The repo already performs that promotion **by hand** (pre-commit secret guard, pre-push E2E gate) with no signal for _which_ rule to promote next. Trial-eval points: only 10 of 36 rules are judgeable (prohibition-shaped), so the scorecard is a partial view; scoring is token-presence heuristics, not semantic compliance, so "violations" need reading, not obeying; and it adds hook overhead on every edit. → 🟤 filed.                                                                           |
| 2026-09-02 | `github` — and with it the whole **External integrations** category (official) | store: [docs](https://code.claude.com/docs/en/discover-plugins#external-integrations) · `github@claude-plugins-official` | pass | The six-day window since 2026-08-27 left the official roster **unchanged** — re-read first-hand, not assumed: code-intelligence LSPs, external integrations, `security-guidance`, development workflows, output styles. So this run reviews the one category that had never been looked at rather than re-reviewing a picked-over list. Its twelve members are `github`, `gitlab`, `atlassian`, `asana`, `linear`, `notion`, `figma`, `vercel`, `firebase`, `supabase`, `slack`, `sentry`; eleven have no counterpart in a solo, local, no-CI, single-developer Electron project with no issue tracker, no design system and no deployed infrastructure. `github` is the only member with real surface here — and it loses to the tool already in use: `gh` is used daily for PRs, and this batch's own methodology rule #3 prescribes `gh pr view <N> --json files` by name. An MCP server buys nothing `gh` does not already do and charges **per-turn context for every turn of every session**, a cost the same doc now makes visible in the `/plugin` Discover tab. Pass, and the category is now closed rather than perpetually unexamined. **Second consecutive run in which a secondary roundup asserted an official plugin that does not exist**: this week's searches named an Anthropic **"Frontend Design"** plugin (as 2026-08-27's did), plus **Semgrep** and **Chrome DevTools**, none of which appear anywhere in the primary roster. Recorded, not carried. |
| 2026-09-02 | `fracalo/electron-playwright-mcp` (community) | wider: [repo](https://github.com/fracalo/electron-playwright-mcp) | pass | **The first §1b candidate ever to clear this section's standing Electron gap** — three prior picks (`playwright-cli-agents`, Dev Browser, the Playwright MCP itself) were all passed or deferred because they drive a *browser* and this repo's E2E target is Electron. This one launches an Electron executable directly and bridges Playwright over MCP: `take_screenshot`, `snapshot`, `evaluate`, `click`, `console_messages`, `network_requests` — precisely the visual-evidence capability § 5's visual-verification trial went looking for this same week. It still passes, on two independent grounds, either sufficient. **(1) Unvetted, at a scale that is not a nitpick**: **1 star, 15 commits, ISC** — and the thing it would be granted is arbitrary control of an app whose own security posture is `sandbox: false` with file **move and delete** IPC. That is the worst possible place to spend trust on an unknown dependency. **(2) The capability is already in this tree** — verified, not assumed: `tests/e2e/helpers/rdp-preload.cjs` and `electron-wrapper.cjs` already solve the Electron-30+ `--remote-debugging-port` problem that makes this hard, and `launchApp()` already drives the real app under Playwright, where `page.screenshot()` is one call away. Nothing external is needed. **This is the run's most useful negative result**: the visual-verification practice did not fail for want of a tool, so adding a tool would not have fixed it — see § 5 and the §3 row, which file the single 🟤 that actually addresses it. Runner-up parked: TestDino's "Playwright Electron Debugging" skill pack, same conclusion, unreviewed. |

### Next-up (parked runners-up)

- **commit-commands** (official) — git commit/push/PR-creation workflows; marginal (already done by hand). Source: code.claude.com/docs/en/discover-plugins
- **Playwright plugin** (Microsoft, official) — browser-automation MCP; web-oriented, low Electron fit. Source: claude.com/plugins/playwright
- **yusuftayman/playwright-cli-agents** (community) — Playwright E2E generation/debug (Page Object Model); Electron-support gap. Source: github.com/yusuftayman/playwright-cli-agents
- **Dev Browser** (Sawyer Hood, community) — drives a browser to self-verify work (Playwright API + DOM computer-use); **web-browser oriented → same Electron gap** as the picks above (this repo's E2E target is Electron). Source: firecrawl.dev/blog/best-claude-code-plugins
- **plugin-dev / agent-sdk-dev** (official) — toolkits for authoring plugins and Agent-SDK apps; no current need (this repo consumes plugins, it does not publish them). Source: code.claude.com/docs/en/discover-plugins
- **`explanatory-output-style` / `learning-output-style`** (official) — output-style plugins; presentation-only, no workflow gap here. Source: code.claude.com/docs/en/discover-plugins
- _Sourcing note (2026-08-27): "best plugins" searches return aggregator roundups, and one asserted an Anthropic **"Frontend Design"** plugin as the most-installed official plugin (~277k). That name appears **nowhere** in the official marketplace roster on `code.claude.com/docs/en/discover-plugins`, whose categories are code-intelligence LSPs, external integrations, `security-guidance`, development workflows, and output styles. Verdicts rest on the primary doc; the roundup claim is recorded as unverified, not carried._
- **TestDino "Playwright Electron Debugging" skill pack** (community) — a skill-pack entry covering Electron app testing among ~70 Playwright guides; surfaced 2026-09-02 as the runner-up to `electron-playwright-mcp` and passed over for the same two reasons (unvetted third party; the capability is already in `tests/e2e/helpers/`). Parked rather than logged — it was never reviewed on its own merits. Source: testdino.com/blog/playwright-skill-claude-code
- _Sourcing note (2026-09-02) — **the roundup problem is now a pattern, not an incident.** Last run caught one aggregator asserting a nonexistent Anthropic "Frontend Design" plugin; this run's searches asserted **the same one again**, plus **Semgrep** and **Chrome DevTools**, as official plugins. The primary roster at code.claude.com/docs/en/discover-plugins contains none of the three. Treat every roundup as a lead to check against the primary doc, never as a source for a verdict._
- _Already reviewed (excluded — see log above): pr-review-toolkit (adopt), test-writer-fixer (defer), Electron Developer Agent (pass), security-guidance (adopt), dead-rules-audit (adopt), `github` + the whole External-integrations category (pass, 2026-09-02), fracalo/electron-playwright-mcp (pass, 2026-09-02)._
- _Already in use here (excluded as already-adopted): Superpowers, Code Review, feature-dev, Context7, Playwright MCP, pr-review-toolkit, security-guidance, dead-rules-audit (installed but its score is not to be used — see § 5)._

---

## 2. Claude best-practices

Top not-yet-reviewed practice/experience for Claude, Claude Code, Claude Design, Claude Cowork, etc.

### Reviewed log

| Date       | Item                                                                                                                                                     | Source                                                                                              | Verdict   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-26 | TDD Guard (hook-enforced TDD)                                                                                                                            | [steering blog](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) | defer     | Hook-based automatic TDD enforcement fits the project's TDD discipline (currently by-convention via superpowers). Surfaced in the unverified tier (session limit cut adversarial verification) and adds a hook dependency — eval before adopt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-05 | Autonomous end-to-end / visual verification before "done"                                                                                                | [best-practices](https://code.claude.com/docs/en/best-practices)                                    | **adopt** | "Give Claude a way to verify its work" — drive the running app + show visual/behavioral evidence (screenshot / computed-visibility), not just a green unit/hook run, before claiming a UI change done. Targets this repo's recurring "passes tests yet invisible/broken" class (the test-actual-visibility incident; the "green hook ≠ green E2E" Continue-resume regression that shipped silently since PR #55). Partially covered by the verify/run skills + E2E; the routine visual-evidence step is the increment → 🟤 filed (possible-dup-of CW-P's E2E-gate item).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-27 | Route instructions by primitive: enforce → hooks, contextual knowledge → skills, **path-conditional → path-scoped rules**, always-on → a short CLAUDE.md | [steering blog](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) | **adopt** | The primary source states two rules verbatim: _"'Every time X, always do Y' in CLAUDE.md — if the behavior should happen reliably … use a hook in `settings.json` instead"_, and _"Keep CLAUDE.md under 200 lines, give it an owner, and review changes to it like code"_, with the explicit escalation that **as the file grows, conventions migrate to path-scoped rules and procedures to skills, where they load only when needed**. Adopted for the **path-scoped-rules half only** — the other halves are already practiced (the hook-promotion rule has shipped twice, as the pre-commit secret guard and the pre-push E2E gate; skills are in daily use). Measured, not assumed: `CLAUDE.md` is **205 lines** (over the doc's own 200-line bar, and matching the already-filed 🟤 [2026-07-03] soft-cap overshoot), and **`.claude/rules/` does not exist** — so every path-conditional block is launch-loaded into every session regardless of relevance. Concrete candidates to migrate: the Vitest/Playwright testing conventions (`tests/**`), the E2E Electron-wrapper gotchas (`tests/e2e/**`), the worker conventions (`*-worker.js`), the preload security note (`preload.js`). The sibling `claude-code-universal-config` shipped exactly this migration for itself (branch `feat/g2-path-scoped-rules-and-claude-md-sizing`), so the pattern is proven in-house. → 🟤 filed. |
| 2026-09-02 | **Audit installed plugins for context cost and disuse**, using the built-in `Context cost` estimate, `Last used` line and **Not used recently** group in the `/plugin` Installed tab | [discover-plugins](https://code.claude.com/docs/en/discover-plugins#manage-installed-plugins) | **adopt** | Chosen over both parked items on measured relevance to a decision **this run just made**. § 5 resolves `dead-rules-audit` to `drop`-as-a-compliance-tool while **leaving it installed**, and § 1 records that this repo now runs Superpowers, Code Review, feature-dev, hookify, `pr-review-toolkit`, `security-guidance`, `dead-rules-audit`, Context7 and Playwright MCP. Every one of those charges startup and per-turn context in **every session**, whether or not it is used — and the repo has never once measured that. The primary doc now makes it measurable without any tooling of ours: the Discover tab shows a per-plugin **Context cost** in tokens, the detail view shows **Last used**, and Claude Code groups plugins unused for two weeks across ten or more sessions under **Not used recently** (an LSP plugin counts as used when its server answers, and theme/output-style plugins are exempt, so the group does not misreport those). This is the accounting side of the same instinct that adopted the path-scoped-rules migration on 2026-08-27: keep launch-time and per-turn cost proportional to what is actually used. The parked alternatives lose on being non-actionable: `/clear` between tasks is already practiced (each dev task opens a fresh session), and the ~70%-human-planning expertise finding is read here as validation of the existing brainstorm→plan→execute discipline rather than a change to make. **Trial vehicle** (§ 5 policy b+, required at filing): the **next Weekly-Reviews run**, which records each installed plugin's Context cost and Last used as a table in § 5 and disables or uninstalls anything that is both expensive and unused — starting with `dead-rules-audit`, whose keep-installed disposition this measurement is the check on. → 🟤 filed. |

### Next-up (parked runners-up)

- **Work on one feature at a time / incremental progress** (verified 3-0) — already practiced here via per-task plans. Source: anthropic.com/engineering/effective-harnesses-for-long-running-agents
- **`/clear` between unrelated tasks** for context hygiene (context fills fast, performance degrades) — confirmed recommended by the best-practices doc, but largely already practiced here (each dev task starts a fresh session per the REMEMBER→PLAN cycle). Source: code.claude.com/docs/en/best-practices
- **Expertise findings: the human should own ~70% of planning decisions** — Anthropic's own usage research finds expert sessions run 2× longer action chains carrying 5× the output, recover from failure at 80–81% vs 60% for novices, and that success correlates with _command of the domain_ rather than coding ability. Read as validation of this project's spec-first / brainstorm→plan→execute discipline rather than a new practice; parked in case a future run finds an actionable increment. Source: anthropic.com/research/claude-code-expertise
- _Reviewed 2026-07-05 (removed from Next-up): "Autonomous end-to-end verification" → adopt (see log above)._
- _Considered and not promoted 2026-09-02: both parked items were weighed against the fresh check per rule #5 and both lost on **actionability**, not on merit — `/clear` is already practiced (a fresh session per dev task), and the ~70%-human-planning finding remains validation of existing discipline with no increment to make. They stay parked rather than being logged as `pass`, since neither was reviewed on its own merits. **Third consecutive run in this category** (2026-07-05, -08-27, -09-02) in which the fresh check beat the parked list; if a fourth does the same, consider whether parking a "runner-up" that is really a non-candidate is worth the line. Note this does **not** hold across sections — §1's 2026-08-27 `security-guidance` adopt was a parked item the fresh check reversed, which is rule #5 working exactly as intended._

---

## 3. Non-Claude AI best-practices

Same, for AI models/tools other than Claude.

### Reviewed log

| Date       | Item                                                                                                             | Source                                                                                                                                                                                                    | Verdict | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-06-26 | Local-model code review (Continue.dev + Qwen3-Coder-Next via Ollama)                                             | [guide](https://overchat.ai/ai-hub/best-local-llm-for-coding)                                                                                                                                             | defer   | Free/private second-opinion review pass complementing Claude `/code-review`; blocked on a capable local GPU (~24 GB) — a real hardware dependency for a solo dev.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-05 | Addy Osmani's incremental LLM coding workflow (2026)                                                             | [blog](https://addyosmani.com/blog/ai-coding-workflow/)                                                                                                                                                   | pass    | Validates current practice rather than extending it — spec-first, iterative chunking, CLAUDE.md rules files, granular commits, mandatory human verification, CI/lint quality gates are all already core to this project's superpowers flow. His most-distinctive technique (frequent commit "save-points") is already practiced (per-task commits) and now also covered by Claude Code checkpoints / `/rewind`. Only non-practiced element (swap LLMs when one stalls) is low-fit for a Claude-centric solo workflow. Nothing new to adopt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-27 | **Harness engineering** — the provider-neutral 2026 successor to context engineering (`Agent = Model + Harness`) | [Martin Fowler](https://martinfowler.com/articles/harness-engineering.html) · secondary: [faros.ai](https://www.faros.ai/blog/harness-engineering) · [arXiv 2602.14690](https://arxiv.org/pdf/2602.14690) | pass    | The clear cross-tool theme of 2026 and explicitly provider-neutral, with a real supporting datum (LangChain moved a coding agent 52.8% → 66.5% on Terminal-Bench 2.0 by changing the _harness_, not the model). Passed because its prescriptions map onto machinery this repo **already runs**. Fowler's taxonomy is _guides_ (feedforward) + _sensors_ (feedback): guides = linters/type-checkers/structural tests, an AGENTS.md-style doc, architecture guidelines, how-to skills → here ESLint + Prettier, `CLAUDE.md`/`PROJECT.md`/`docs/`, superpowers skills; sensors = static analysis on agent output, AI-assisted review, coverage → here the pre-commit chain (secret scan → lint-staged → 513 unit tests), the pre-push E2E gate, `/code-review`. Its headline principle, _"shift feedback left — cheap deterministic checks pre-commit rather than post-integration"_, is precisely what PR #63 built. Fowler is also candid about the limits: **no quantitative evidence** for the benefit claims, computational sensors "catch style violations reliably" while neither sensor class reliably catches misdiagnosis or misunderstood requirements, and the behavioral-harness category "remains largely unsolved". Same shape as the 2026-07-05 Addy Osmani `pass` — validates current practice rather than extending it. One genuinely unpracticed layer parked below. |
| 2026-09-02 | **Loop engineering / evidence-gating** — "DONE, tested, reviewed and ready-to-merge are **claims** until current evidence supports the transition"; an agent loop's stop rule must be a check that can fail | [Addy Osmani via KyenAI](https://www.kyenai.com/guides/loop-engineering-ai-coding-agents) · [AI Builder Club](https://www.aibuilderclub.com/blog/loop-engineering-guide-2026) · [futureproofing.dev](https://www.futureproofing.dev/resources/ai-native-team/agentic-coding-workflow-2026) | **adopt** | The 2026 cross-tool successor to last run's harness-engineering theme, and provider-neutral. It would ordinarily be a `pass` here for the same reason Fowler and Osmani were — the repo already runs `superpowers:verification-before-completion`, a pre-commit chain, a pre-push E2E gate and `/code-review`. **It is an adopt because this run measured the norm failing.** § 5's visual-verification read-out found that G5 shipped a progress-card change — a thing whose whole purpose is to be looked at — with **zero** visual evidence, past five reviewers and an adversarial pass, while the practice sat adopted-and-unenforced since 2026-07-05. The literature names exactly that failure: a stop rule that is a *norm* rather than a *check that can fail* is not a stop rule. The adopted increment is therefore the **gate half only** — convert the visual-verification norm into a deterministic check on machinery this repo already owns (`launchApp()` + `page.screenshot()` in `tests/e2e/helpers/`), which §1b established needs no new tool. The half already practiced (tests, lint, hooks as feedback signals) is not re-adopted. Corroborating evidence from this same run, and the reason the claim is not merely rhetorical: every rule this repo has ever successfully promoted became **deterministic** — the pre-commit secret guard, the pre-push E2E gate, `check-docs-index.js` — while every advisory one recurred until it was gated. **Trial vehicle** (§ 5 policy b+): the single 🟤 filed below, which is **shared with § 5's visual-verification re-trial and deliberately not double-filed** — one entry, two rows pointing at it. → 🟤 filed. |

### Next-up (parked runners-up)

- **Spec-Driven Development / GitHub Spec Kit** (90k★, v0.11.0 Jun 2026; human-refined specs cut LLM code errors up to ~50%) — _already practiced here_ via the superpowers brainstorm→writing-plans→executing-plans flow, so no new tool needed (validates current workflow). Source: github.com/github/spec-kit
- **Harness _observability_** — the one layer of the five-layer harness model (tool orchestration · verification loops · context & memory · guardrails · **observability**) this project does not implement: no measurement of _where_ the agent actually fails, so rule-promotion and process fixes are chosen from recollection rather than data. Deliberately not adopted this run — the 2026-08-27 `dead-rules-audit` adopt is a narrow, concrete instance of exactly this idea (measure which rules get ignored), so trial that first and revisit the general layer only if it earns its keep. Source: martinfowler.com/articles/harness-engineering.html · faros.ai/blog/harness-engineering
- _Reviewed 2026-07-05 (removed from Next-up): "Addy Osmani's incremental LLM workflow" → pass (see log above)._
- **Harness observability — gate condition resolved 2026-09-02, and the answer was no.** The parked note said "the 2026-08-27 `dead-rules-audit` adopt is a narrow, concrete instance of exactly this idea (measure which rules get ignored), so trial that first and revisit the general layer only if it earns its keep." It did not earn its keep: § 5 resolves it `drop`, and not for a cost or a fit reason but because its measurement was **invalid** — it scored compliant edits as violations and nominated the repo's best-followed rules for promotion. That is a stronger result than a plain failure, and it argues **against** the general layer rather than merely deferring it: the hard part of observability is not collecting the signal, it is establishing that the signal means what it claims, and a homegrown instance of the same idea would face the identical burden with no upstream to inherit it from. Stays parked, now with a measured reason instead of a pending condition. Revisit only with a proposal that says how its metric would be **validated**, not what it would count._

---

## 4. Cross-project propagation

**Added 2026-08-27** (WEEKLY.md G5). Unlike sections 1–3, this section's candidates are **not external
web hits** — they are learnings crossing a repo boundary. The conventions below are **imported** from
the sibling repo `claude-code-universal-config` (its `docs/planning/REVIEW-QUEUE.md`; a sibling checkout,
deliberately not linked — a relative path would resolve above this repo root and 404 in any clone),
which has run an equivalent section four times (2026-07-03, -07-16, -07-21, -07-27), rather than
invented here — one convention, two directions.

### Conventions

- **Verdict vocab is `propagate | pass | defer`** (not `adopt`).
- **Row (outbound)**: `YYYY-MM-DD — <learning/origin> — verdict: … — note`.
- **Row (inbound)**: `YYYY-MM-DD — <origin repo / source> — <per-item status> — note`. An inbound
  sweep covers several items at once, so the status is stated **per item** inside the row rather than as a
  single trailing verdict.
- **`propagate` → file a row in [`TODO.md`](TODO.md) § Spawned Tasks** — the outbound analog of
  `adopt` → 🟤 BACKLOG. `pass`/`defer` stay in this file.
- **Verify against the target; never assume.** Confirm a learning is absent from the destination by
  grepping the specific strings there. Diffing trees is meaningless — they are scrubbed and drift by
  design.
- **Direction (D1, this repo's deviation).** WEEKLY.md defines only the outbound half. This section is
  **bidirectional**, because the sibling repo already carried unactioned rows addressed to
  media_viewer that a one-way channel would have dropped permanently.
    - **Outbound** — what shipped _here_ since the last run that belongs elsewhere.
    - **Inbound** — what another repo has already routed _at_ this one. An inbound item is actioned only
      once its **origin repo's own review is complete**; until then it is recorded and parked, not filed
      as work.
- **Target scope (D2)**: `~/.claude` via `claude-code-universal-config` is the one **confirmed**
  standing target. `wallpaper_sorter`, `media_compression`, `media_parser` are **TBD** — not yet
  established as propagation partners; decide before treating them as targets.
- **Scan window** = since the previous run's date, not "this week".

### Reviewed log — outbound

<!-- newest last: YYYY-MM-DD — <learning/origin> — verdict: propagate | pass | defer — note -->

2026-08-27 — This window's shipped work (PRs #63 CW-P / #64 G1 / #65 G2 / #66 G3, since the 2026-07-05 run) — verdict: **propagate** — scanned and **verified against the targets by grep, not assumed**. The propagated learning is the **code-review rating axis: score by _realness_, not by severity or frequency.** It is the most-recurring process finding this repo has produced — it fired on **four consecutive PRs** (#59, #64, #65, #66) — and it has one measured cost: the sub-threshold lifecycle finding deferred on PR #64 went on to destroy a **23,559-entry / 126 MB** feature cache of real user data. Two clauses: (1) a confirmed defect must not be dropped because it is hard to hit, cheap to fix, or comment-only — frequency and recoverability are _not_ realness; and (2) a shared-state/lifecycle race that pre-exists a PR becomes that PR's finding when the PR adds a **new consumer** whose correctness depends on the broken invariant. Both currently live only in this project's per-project memory files (`feedback_defer_lifecycle_findings.md`, `feedback_review_threshold_comment_findings.md`), which reach no other project by any route. **Absence at the target was tested, not assumed**: `realness` / `severity axis` / `confirmed bug` return **0 hits** in both `~/.claude/POLICIES/code-review.md` and the synced source `home-claude/POLICIES/code-review.md`. The single adjacent hit — global `POLICIES/code-review.md` L266, _"anything the reviewer raised below its own reporting threshold and passed on informally rather than posting"_ — is **complementary, not redundant**: it governs _bookkeeping_ (a sub-threshold remark still gets a bullet in the response record), whereas this learning is upstream of it (_don't let the filter drop a confirmed bug in the first place_). Routing note: the target is the **synced product**, so the follow-up is a two-trees edit (`home-claude/` + live `~/.claude/`), which § Spawned Tasks explicitly anticipates. Spawned → [TODO.md](TODO.md) § Spawned Tasks. One runner-up parked below. Nothing else in the window propagates: G1's incremental-cache-load, G2's unified undo stack and G3's deferred-refresh protocol are all media-viewer-specific implementation, and their generic residue (_"a green suite hid both of the design's core assumptions"_, _"the E2E passed for the wrong reason"_) is already covered globally by the existing verification policy.

2026-09-02 — This window's shipped work (G1 `66b16af`, G2 `6305a7a`, G3 `45a0d9b`, G5 `0d3fed5`, G4 closeout `bf58c01`, since the 2026-08-27 run) — verdict: **propagate** — the promoted item is the parked **"a reviewer's _negative_ finding must cite the evidence that establishes it."** A confident denial is worse than a miss: a miss leaves the question open, a denial closes it. Chosen over the other parked candidate (the pre-push E2E gate as a no-CI pattern) because its prerequisite is far narrower — it applies to any review, where the gate presupposes a project that has an E2E suite _and_ no CI — and because it fired **again inside this very window**: the G5 closeout review produced a comment asserting a false premise that three separate comments then restated, and the fix (`e2b5724`) was to delete the line rather than bless it. Target is global `POLICIES/code-review.md`, a **two-trees edit** (live `~/.claude/` plus the synced `home-claude/` source), so it routes to TODO § Spawned Tasks rather than a 🟤 entry. **Absence at the target was tested, not assumed**: `negative finding` / `cite its evidence` / `unevidenced` / `confident denial` / `denial` return **0 hits** in both `~/.claude/POLICIES/code-review.md` and `claude-code-universal-config/home-claude/POLICIES/code-review.md` (382 lines each; both confirmed to exist, so the zero is a real absence and not a missing-file artefact). The pre-push E2E gate stays parked with its own grep evidence intact.

⚠️ **Burn-down warning recorded with this row — `propagate` has the same disease `adopt` had.** While verifying the target, this run also grepped for `realness`, the learning the **2026-08-27** run propagated, and got **0 hits** in `~/.claude/POLICIES/code-review.md`. Its TODO § Spawned Tasks row is still unchecked. So the outbound channel is now **2 propagations, 0 applied** — precisely the pattern the 🟡 [2026-08-27] entry diagnosed for adopts ("a review process whose output is never tried produces verdicts, not value") and that § 5 was created to fix, reappearing one section over and unnoticed because § 5's trial log covers `adopt` only. This is not an argument against filing the second one — the user chose it, and the target genuinely lacks it — but a run that files a second propagation without saying the first was never applied is repeating the mistake it already paid for once. Two structural options for the next planning conversation, not decided here: extend § 5's trial-log discipline to cover `propagate` (an outcome row per propagation, so an unapplied one is visible), or accept that § Spawned Tasks rows are user-maintained and out-of-tree, and stop treating "filed" as "done" in these notes. Filing rate is fine; **application rate is zero**.

### Reviewed log — inbound

<!-- newest last: YYYY-MM-DD — <origin repo / source> — <per-item status> — note -->

2026-08-27 — `claude-code-universal-config` § Spawned Tasks → **4 rows reaching media_viewer**, none of which had ever arrived in this repo's planning docs. Precisely: **3 are media_viewer-dedicated** and the 4th (`dead-rules-audit`) is a **shared row targeting four projects** (`social_stats`, media_viewer, `goodalex223`, `rating_bot`) on which media_viewer is named the strongest fit — which is why its adopt rests on this repo's own 36-rules/10-judgeable measurement rather than on the row itself. This is the first inbound sweep, and it is the reason D1 made the section bidirectional. Status per row: (1) **`dead-rules-audit`** — upstream-confirmed 2026-07-27 with a measurement against _this_ repo's `CLAUDE.md` (36 rules / 10 judgeable) → **consumed by this run's §1 Plugins `adopt`**; deliberately **not** double-filed as a separate propagation task. (2) **`/wayfinder` planning skill** (Matt Pocock, video `F3lL98Pj90o`), (3) **Jenkins automation server** — routed here explicitly for _"CI/CD, scheduled jobs"_, which lands on this repo's known and documented **no-CI** gap, and (4) **design-themed Claude video sub-batch** (`NWDuC-w1lwA`, `7FU98O0JLHs`, `K2_qHRcbNtU`, `Pe-ubc8ypis`, Chase AI). Rows 2–4 all carry an upstream marker that _the origin repo's own REVIEW-QUEUE review is still pending_ (🔵 [2026-08-24] there) — so per the inbound rule they are **recorded and parked, not filed as work**; importing an unvetted recommendation would launder the origin's uncertainty into this repo's backlog. Re-check them once the origin's verdicts land.

2026-09-02 — `claude-code-universal-config` (re-check of the three items parked 2026-08-27) — **all three still pending upstream; all three stay parked, unchanged.** Verified by reading that repo directly rather than trusting last run's note: its HEAD is `7dbfd15` (PR #46 merged), and (1) **`/wayfinder` planning skill** is still an unchecked `- [ ]` in its `docs/planning/BACKLOG.md` (the review-the-video entry), (2) **Jenkins** is still an unchecked `- [ ]` ("Evaluate where Jenkins could be useful"), and (3) the **design-video sub-batch** (`NWDuC-w1lwA` and its three companions) is still unreviewed there. Its own `docs/planning/TODO.md` rows continue to carry the "this repo's own REVIEW-QUEUE review still pending, 🔵 [2026-08-24]" marker verbatim. Per the inbound rule an item is actioned only once its origin's review is complete, so nothing is filed here for the **second consecutive run**. Observation, not an action: these have now been parked across two runs and ~six weeks. The rule is right — filing them early would launder the origin's uncertainty into this repo's backlog — but if a third run finds them unchanged, the honest response is to ask the origin repo directly rather than to keep re-checking on a weekly cadence that is not the one driving the answer.

### Next-up (parked)

<!-- outbound candidates spotted but not yet propagated; inbound items awaiting their origin's verdict -->

- _Outbound_ — **The pre-push E2E gate as a general no-CI pattern** (PR #63: `scripts/check-e2e-needed.js` + `.husky/pre-push`, with a code-aware docs-only skip). Verified absent at the target: `pre-push` returns **0 hits** across global `CLAUDE.md`, `WORKFLOW.md` and `POLICIES/`, and across the synced `home-claude/` product (its only hits are session transcripts under `projects/`/`file-history/`, not policy); global `WORKFLOW.md` has **0** mentions of E2E at all. Held back this run because its audience is narrower than the propagated item — it presupposes a project with an E2E suite _and_ no CI — and because a second propagation in one window would exceed the sibling repo's established one-per-run cadence. Promote next run if it still fits. **Held again 2026-09-02** — the same one-per-run cadence, spent this time on the negative-finding rule, whose prerequisite is narrower. Its grep evidence stands unrefreshed and is now ~1 week old; **re-verify absence before promoting**, do not carry the 2026-08-27 zero forward. ⚠️ Weigh the promotion against the burn-down warning recorded on this run's outbound row: two propagations are filed and **neither has been applied**, so a third filing buys less than applying one would.
- _Inbound_ — **`/wayfinder` planning skill**, **Jenkins (CI/CD for the no-CI gap)**, **design-video sub-batch** — all three awaiting `claude-code-universal-config`'s own REVIEW-QUEUE verdicts (🔵 [2026-08-24] there). Re-check on the next run; do not action earlier. **Re-checked 2026-09-02 against that repo at `7dbfd15`: all three still unchecked upstream, all three stay parked** (see the dated inbound row above). Second consecutive run with no movement — if a third finds them unchanged, ask the origin repo directly rather than re-checking on a cadence that is not the one producing the answer.
- _Consumed 2026-08-27 (removed rather than struck through): `dead-rules-audit` → §1 Plugins `adopt`._
- _Consumed 2026-09-02 (removed rather than struck through): "a reviewer’s negative finding must cite its evidence" → §4 outbound `propagate`, routed to TODO § Spawned Tasks._

---

## 5. Adopt trials

**Added 2026-08-30** (WEEKLY.md G4). Sections 1–3 produce `adopt` verdicts; until now nothing recorded
what happened next, so an adopt had no terminal state. This section closes that loop. Frozen
Reviewed-log rows are never rewritten — a trial outcome lands here and cross-refers to its verdict date.

### Policy (decided 2026-08-30 — option **b+**)

- **An `adopt` MUST name its trial vehicle** — the specific PR, session, or measurement that will
  exercise it — at the moment the verdict is filed. A candidate with no nameable vehicle is filed
  `defer`, not `adopt`. Reviews stay hands-off (D4 unchanged: file the entry, install nothing); what is
  new is that a verdict must state _how it would ever be consumed_.
- **Trials run in batches**, in Cleanup Weeks by default, and each writes a row to the log below.
- **Queue cap**: while more than **3** adopts are outstanding (filed, no outcome row), a new candidate
  is parked under its section's **Next-up** instead of being filed as `adopt`.
- **Not every `adopt` is a trial.** An adopt that is simply implementation work with a known shape (the
  path-scoped-rules migration, for one) is ordinary 🟤 BACKLOG work and does **not** belong in this log.
  The log is for candidates whose value is uncertain until exercised — tools, plugins, practices.
- **Cadence: weekly, as written** — reaffirmed 2026-08-30 rather than re-timed to the observed average.
  Recorded as a watch, not a re-litigation: the first three runs landed 2026-06-26 → 07-05 → 08-27,
  three runs in ten weeks. If a fourth long gap opens, re-open the cadence question with that as the
  evidence rather than absorbing the slip.

**Why this exists.** The 2026-08-27 run recorded `adopt` verdicts with zero trial outcomes and diagnosed
it as _"a review process whose output is never tried produces verdicts, not value"_. The measured cause
was not neglect: `pr-review-toolkit`'s vehicle was "a real PR", and the PR it waited on never opened —
G1 merged locally as `66b16af`. A vehicle that names nothing in particular is how an adopt becomes a
permanent resident.

**Corrections to the 2026-08-27 stocktake** (measured 2026-08-30, not assumed):

- The 🟡 BACKLOG entry states "**5 unchecked**" while naming **six** — its own total drops the
  path-scoped-rules migration. BACKLOG carries six unchecked adopt-derived entries.
- `security-guidance` was **already installed and enabled at user scope on 2026-08-24** — three days
  _before_ the run that adopted it — venv built at `~/.claude/security/`. One of the six was therefore
  never outstanding, and it was consumed **outside** the process rather than by it.
- Corrected state at the time of writing: **6 filed · 1 already live · 5 genuinely outstanding**
  — and **4 outstanding** the moment this same commit closes `pr-review-toolkit`.

**Stocktake update (2026-09-02, the first run to both close and open trials).** Two of the three
outstanding trials were read out and closed this run — `dead-rules-audit` → `drop`, visual
verification → `inconclusive` — and two new adopts opened one each, with the visual-verification
re-trial **merged into** the evidence-gating row rather than filed separately. Net: **3 outstanding**
(`typescript-lsp`, the plugin context-cost audit, evidence-gating), unchanged in count and exactly at
the cap, so a new candidate next run parks in its section's _Next-up_ rather than being filed `adopt`.
Worth stating plainly, because the count hides it: this is the first run where the queue actually
turned over rather than only growing, and the policy that made it turn over was **D4 ordering** —
reading out the due trials before sourcing new ones, so §§1–3 met a lifted cap instead of a full one.
Also noted here because § 5 covers `adopt` only and nobody was watching the other channel: §4's
`propagate` verdicts have the **same** zero-burn-down problem this section was built to fix — 2 filed,
0 applied. See the warning recorded on that section's 2026-09-02 outbound row.

### Trial log

<!-- newest last: | date | candidate (verdict date) | vehicle | outcome: keep / drop / pending / inconclusive | note | -->

| Date | Candidate (verdict) | Vehicle | Outcome | Note |
| ---- | ------------------- | ------- | ------- | ---- |
| 2026-08-30 | `security-guidance` (§1, 2026-08-27) | none — retroactive; it was already live before its own verdict | **keep** (in use since 2026-08-24) | The prerequisite check the 🟡 entry asked for, now measured on this box: **Python 3.13.5** on `PATH` (`python`/`python3`/`py` all resolve) ✅, venv built at `~/.claude/security/` ✅, win32 ✅ — the "unverified on this Windows/Node box" caveat is retired. Installed **user scope 2026-08-24**, enabled in `~/.claude/settings.json`, 56 session-state files and ~1.9 MB of hook logs, observed firing on `PostToolUse` during the 2026-08-30 G4 session itself. Still unmeasured: the token cost of its model-backed layers, and it sees only commits Claude makes via Bash. |
| 2026-08-30 | `pr-review-toolkit` (§1, 2026-06-26) | G1's pre-review-round diff `29636fd..9aa27d1`, judged against the `/code-review` findings recorded for that exact revision | **keep** (complement, not a replacement) | Four agents (code-reviewer, silent-failure-hunter, pr-test-analyzer, comment-analyzer) run **blind** — no baseline shown — on the same revision, in a throwaway worktree so they saw pre-fix code. **Against the baseline**: it re-found the `loadFolder` race (all four, with the same mechanism, including that `showLoadingSpinner()` does not set `isLoading`) and the CLAUDE.md `showMedia()` wording — and **extended** the latter, catching that "called in *all* `handleCancel` branches" is *also* false, the half the baseline fix left standing, so the live doc is still wrong on `main` today. It also re-found the E2E debounce flake vector the baseline raised, and three of the four independently re-found the already-filed `scores: null` stall. **Beyond the baseline**: 8 items filed as 🟤 [2026-08-30], each re-verified by me against current `main` before filing — four are live lifecycle defects the 5-reviewer baseline missed, headed by `moveComparePair` hardcoding `pendingCompareUpdates = 2` while discarding both post results (**three-way independent convergence**), which violates the very invariant G1 added to CLAUDE.md; and `loadFolder`'s two failure exits, which still leak a window armed during the scan — the residual of the exact race the baseline's own fix narrowed. **Cost**: ~613k subagent tokens across the four, ~13 min wall-clock in parallel. **Verdict rationale**: it does not replace `/code-review` (no scoring pass, no threshold discipline, and it will happily report pre-existing and already-tracked issues), but on a lifecycle- or test-heavy diff it demonstrably finds what a scored 5-reviewer pass filtered out. **Use it for**: diffs touching shared-state lifecycles or adding non-trivial tests, as a second pass before the scoring review. **Operating notes**: give it an explicit `git diff A..B` target (its default is unstaged work), use a worktree for a historical revision, tell it not to mine `docs/archive/plans/` or it will restate the author's own notes as findings, and expect cost to scale linearly with agent count — each agent re-reads the whole diff. `type-design-analyzer` and `code-simplifier` were not run (untyped JS; the simplification axis belongs to `/simplify`). **⚠️ Post-trial correction (2026-08-30, code review of this very commit)**: all eight filed items were re-derived from source, and parts of four were overturned — item 1's stated trigger is unreachable (the feature-capture block is itself gated on `isMlEnabled && mlWorker`, leaving a narrower TOCTOU), item 3's predicted symptom was wrong (`showTournamentPair` sets `isCompareMode = true`, so the orphaned render is a wrong-*pair* re-render, not a stray single), item 4's headline was refuted (`window.onerror` **does** forward to `logError`), and item 7 understated existing coverage. **That is the sharper verdict on this tool than "8 items beyond baseline"**: every claim that failed failed in the *same direction* — a real defect wrapped in a confident, unverified reachability story — while the four that stayed with what the code literally says (2, 5, 6, 8) all held, including the subtle hash-ordering one. **Operating rule that follows**: treat its output as leads with mechanisms attached, and verify the *mechanism*, not just the defect. Note too that item 1's three-of-four agent convergence was convergence on a mechanism — it is not evidence of reachability, and reading it as such is how the unreachable trigger got filed. |
| 2026-08-30 | `dead-rules-audit` (§1, 2026-08-27) | the G2 + G3 implementation sessions (Sep 1–3), both edit-heavy; scorecard read out at G6, Fri Sep 4 | **drop** as a compliance tool — kept installed, read only as a relevance heat-map (read-out 2026-09-02) | **READ-OUT 2026-09-02.** The read-out rule below guards the wrong failure. The vehicle worked: the ledger holds **13 sessions, 6,499 relevance hits, 2,134 judgements, 1,136 "violations"** — not zero. The **scoring** is what fails. Measured by running the plugin's own `parseRules` + `judge` against this repo's `CLAUDE.md` (32,585 bytes / 211 lines): `ruleKeywords` does not separate a rule's **prescribed** tokens from its **prohibited** ones, so `judge()` marks a rule violated whenever _any_ backticked token from it appears in newly-added live code. Rule 36's token set is `loadfolder`, `exittournamentmode`, `tournament`, `engine` — every one of which the rule instructs you to write. Two snippets obeying rules 36 and 37 verbatim both returned `{"relevant":true,"violated":true,"judged":true}`. The metric is therefore close to **inverted** for prescriptive "do X, never Y" rules, which is how most Active-gotchas here are written. **The `⚠ promote→hook` flag proves it**: 10 of 43 rules are flagged, headed by #36 (150/247) and #37 (141/275) — the two tournament rules G2 *shipped and enforced* this very week. A plugin adopted to find chronically-ignored rules is nominating the repo's best-followed ones. A hook-log line from 2026-09-02 shows the same thing without a synthetic probe: an edit to `docs/archive/plans/README.md` — a **Markdown file** — scored relevant to 36 rules and violated 4, including "`mlWorker` is lazy". **Cost is not the objection**: `scoreDiff` measured **10.2 ms** per edit on a 20 KB diff and `parseRules` **3.8 ms** at SessionStart, unnoticeable beside prettier and `security-guidance`. Two secondary defects recorded so they are not re-derived: the ledger holds **43** rules against the current 42 because entries are keyed by a hash of the rule _text_, so editing CLAUDE.md mid-trial orphans rules and resets their history (CLAUDE.md went 206 → 211 lines during G2 + G3, and the parser now reads **42 / 13 prohibition-shaped / 12 judgeable** vs G4's 38 / 12 / 11); and two distinct rules both report `id=35`, in the ledger and in the rendered card's `promote` list alike. **Disposition**: left installed — no dependencies, no measurable cost — with only the `relevant` column used, as a heat-map of which documented rules a week's work actually touched. The compliance percentage and the `⚠ promote→hook` flag are not to be used here. **No re-trial filed**: the defect is in the scoring function, not the vehicle, so another vehicle reproduces the same invalid numbers. Upstream bug report considered and declined as out of scope. _Original pending note follows._ Installed 2026-08-30 (marketplace `karanb192/claude-code-hooks` added; enabled at project scope). Vehicle chosen because the plugin scores `Edit`/`Write` calls per session against CLAUDE.md rules — a docs-only session like G4's own would measure its floor, not its fit. The read-out records: which rules it flags `⚠ promote→hook`, how many of the **38** parsed rules (**11** judgeable — per the re-measurement below, not the figure the 2026-08-27 entry carried) are ever scored, and whether its `PostToolUse` cost is noticeable stacked on the existing prettier and `security-guidance` hooks. **Parser re-measured first-hand 2026-08-30** (its own `parseRules` run against this repo's `CLAUDE.md`): **38 rules parsed — 12 prohibition-shaped — 11 actually judgeable**, not the 36 / 10 the sibling repo reported (CLAUDE.md is **206** lines). "Judgeable" is narrower than "prohibition": the code scores a rule only if it is a prohibition **and** names a concrete backticked token. **8 of the 11 come from the Active-gotchas section** (the other three sit outside it, at CLAUDE.md L97, L133 and L157) **and 3 are tournament-specific** (`handleTournamentUndo` / `engine.history`, `engine.files` vs `strategy.files`, folder-scoped tournament mode) — which is what makes **G2 the load-bearing half of the vehicle**; G3 (scripts + docs) will barely touch a judgeable rule. ⚠️ **Precondition**: the `PostToolUse` matcher is `Edit` / `MultiEdit` / `Write` — **file changes made through Bash are invisible to it**, so a session that edits via shell scores nothing and the scorecard would read as "compliant" when it simply never observed anything. G2/G3 must use the Edit/Write tools for the trial to produce data. **This is not hypothetical: auto mode routes file edits through Bash, and the G4 session that installed the plugin made every one of its own edits that way** — it would have scored zero. Recorded as a callout on the G2 and G3 group headers in [WEEKLY.md](WEEKLY.md), where the sessions will actually meet it, rather than here alone. **Read-out rule**: a scorecard with **zero scored edits is `inconclusive`, never `keep`**. |
| 2026-08-30 | autonomous e2e / visual verification (§2, 2026-07-05) | G5 (AI-sort training-phase progress polish, Thu Sep 3) — this week's only user-visible UI change | **inconclusive** — the vehicle fired, the practice was never performed (read-out 2026-09-02) | **READ-OUT 2026-09-02.** G5 shipped on 2026-09-02 (merge `0d3fed5`) and **produced no visual or behavioural evidence at all**. Verified, not assumed: `git log --name-only` over the G5 range lists only `*.md` and source files — **no image of any kind** was committed; and `docs/planning/DONE.md` greps clean for `screenshot` / `visual` / `ran the app` / `npm start` anywhere in the G5 entry. Its recorded verification is **unit 597 → 613 and E2E 56/56** — precisely the automated coverage this row already said "does not discharge this". So the trial answers a different question than the one it asked. It cannot say whether requiring visual evidence catches defects, because the evidence was never gathered; it says, with a clean measurement, that **the practice does not happen on its own.** A session with a strong review culture, five reviewers and an adversarial pass still shipped a progress-card change — a thing whose entire purpose is to be *looked at* — without once looking at it. That is the same failure mode `dead-rules-audit` was adopted to detect (an advisory rule that is never enforced and so is never followed), observed directly, in the same week, on the trial that was supposed to measure it. **The pairing is the finding**: of the two mechanisms trialled against "advisory rules get ignored", the detector produced inverted numbers and the practice produced nothing, so the only thing that has ever reliably promoted a rule here remains a deterministic gate (the pre-commit secret guard, the pre-push E2E gate, `check-docs-index.js`). **Re-trial filed** — unlike `dead-rules-audit`, the practice itself is untested rather than disproven, and it needs an enforcement point rather than another volunteer vehicle. _Original pending note follows._ A practice, not a plugin: require visual/behavioural evidence (a screenshot or a computed-visibility check) before calling a UI change done. G5 is the honest vehicle — a progress card rendering in a phase the user actually watches. G1's real-worker E2E was previously offered as "the concrete instance", but that is automated coverage, not visual evidence, so it does not discharge this. |
| 2026-08-30 | `typescript-lsp` (§1, 2026-07-05) | next Cleanup Week's trial batch (~3 weeks) — deferred by WEEKLY.md G4 | **pending** | Deliberately not trialled this week: it needs `typescript-language-server` on `$PATH` (unverified here), and stacking a language server on top of two fresh plugin trials would confound all three. Under the queue cap this is the **3rd** outstanding trial — the three `pending` rows in this log. (The path-scoped-rules migration is a repo change, not a tool trial, and § 5's policy excludes it from the count; an earlier draft of this cell reached "4th" by counting it.) A 4th would push past the cap, and new adopts would park in Next-up. |
| 2026-09-02 | installed-plugin context-cost & disuse audit (§2, 2026-09-02) | the **next Weekly-Reviews run**: record every installed plugin's `Context cost` and `Last used` from the `/plugin` Installed tab as a table in this section, then disable or uninstall anything both expensive and unused | **pending** | Vehicle is deliberately the review batch itself rather than a dev session: the measurement is a one-off reading of a built-in UI, not something a coding task would exercise. First subject is `dead-rules-audit` — this run resolved it `drop`-as-a-compliance-tool but **kept it installed**, and that disposition is only defensible while its per-turn cost is near zero, which is asserted from a 10.2 ms `scoreDiff` timing and **not yet from the tab's own token figure**. If the audit shows real context cost, the keep-installed call is revisited on the spot. Second subjects: the nine plugins §1's Next-up now lists as already-in-use. |
| 2026-09-02 | evidence-gating: convert the visual-verification norm into a deterministic check (§3, 2026-09-02) | the 🟤 entry filed under `### [2026-09-02] From: Weekly Reviews` — build the screenshot/computed-visibility step on the existing `launchApp()` + `page.screenshot()` harness, then require it for a UI change | **pending** | **Shared row — this is also the re-trial owed by the visual-verification outcome above, deliberately not double-filed.** The first trial failed for want of a gate, not a tool (§1b established the capability is already in `tests/e2e/helpers/`), so re-running it as another norm would reproduce the same nothing. The trial question is therefore narrower and answerable: **does a deterministic check get run, where the norm did not?** Success is not "the gate found a bug" but "a UI change could not be called done without visual evidence". If this one also produces no evidence, the honest conclusion is that the practice does not fit this workflow, and it should be dropped rather than re-vehicled a third time. |
